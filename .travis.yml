language: node_js
node_js:
  - '11'
cache:
  directories:
    - node_modules
    - functions/node_modules
install:
  - npm ci
  - (cd functions && npm ci)
script:
  - npm run build
after_script:
  - ./travis-deploy.sh
